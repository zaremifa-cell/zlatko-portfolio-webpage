# Generative Canvas Pattern

A full-screen HTML5 Canvas sketch that draws a monochrome, trig-driven geometric pattern and animates it over time.

## Run

Open `index.html` directly in a browser, or serve the folder locally:

```sh
npm run start
```

Then visit `http://localhost:5173`.
